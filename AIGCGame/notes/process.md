# 课程过程记录

游戏设计见 [planning.md](planning.md)、[levels.md](levels.md)。本文只收作业过程：工时、AIGC、条目对照与自测，随开发补全。

---

## 1. PSP

编码时间记入对应功能行；AIGC 行只记额外审阅与对照，避免与功能行重复加总。

| 任务 | 预估（小时） | 实际（小时） | 差异（小时） |
|---|---:|---:|---:|
| 需求分析与游戏设计 | 1.5 | 2.0 | +0.5 |
| Python 与图形库学习 | 1.0 | 1.0 | 0 |
| 游戏界面实现 | 2.5 | 2.0 | −0.5 |
| 路径与碰撞逻辑实现 | 2.0 | 1.5 | −0.5 |
| 关卡设计 | 1.5 | 1.5 | 0 |
| AIGC 辅助开发 | 1.5 | 1.5 | 0 |
| 测试与修改 | 1.5 | 1.0 | −0.5 |
| README 与博客撰写 | 2.0 | | |
| 合计 | 约 13.5 | 10.5（至 I3） | |

> 实际工时以本人计时为准；上表随增量回填，可观测时间线见第 6 节。

---

## 2. AIGC

程序实现与调试以 Cursor 起手、Claude Code 续做；规则边界、关卡是否有解由 Claude Code 独立对照。下表随真实对话追加，不事后虚构。

| 子任务 | 工具 | 提供了什么 | 效果 | 人工修改 |
|---|---|---|---|---|
| 初版结构 | Cursor | 玩法拆解、增量与分层 | 可作骨架，偏说明书口吻 | 按策划案重写，关卡外置 |
| 文档重构 | Cursor | 规则表、流程、增量需求、接口分表 | 待开发中校验是否好用 | 过程类内容从策划案迁出，归入本文 |
| 元认知提示补全 | Claude Code | 从策划案提炼语言 / 组织 / 思想三句底层逻辑 | 可复用为后续任务判据 | 否 |
| pygame 版本核验 | Claude Code | 逐项核验 23 个接口名 + SysFont 实跑 | 确认 2.6.1 无接口名错误；发现 SysFont 运行期 bug | 改 pygame.md 用 `Font(path)` |
| 关卡可解性对照 | Claude Code | 射线脚本验四关参考序与可解性 | 发现 L1/L3 参考序写错 | 改 levels.md 参考序 |
| 架构与 I1 编码 | Claude Code | 状态机+规则/表现分离骨架、rules/view/scenes/anim、levels.json | 规则层单测 7 项、冒烟通过 | 待 ChatGPT 审查 |
| I2 核心循环 | Claude Code | 射线结算接入点选、飞出/受阻动画、动画期锁输入 | 规则层单测 + 冒烟通过 | 待 ChatGPT 审查 |
| I3 关卡闭环 | Claude Code | 结果画面（通过/失败/全通）、清空进下一关、余次耗尽失败、重开按钮 | 规则层单测 + 冒烟通过 | 待 ChatGPT 审查 |
| 扩展与优化分析 | ChatGPT | extension_development_summary + levels_extension（优先级、solver/undo/动画/评分/存档/打包方案） | 可直接实施，含可运行代码 | 否（采纳为主） |
| P0 扩展实现 | Claude Code | E1 关卡 L4–L7+solver、E2 撤销、E3 受阻前冲返回 | 规则层 9 项 + 冒烟通过 | 待 ChatGPT 审查 |
| E4 评分计时 | Claude Code | score.py 评分/星级、PlayScene 计时、结果画面星级、大盘自适应 cell | 规则层 + 冒烟通过 | 待 ChatGPT 审查 |
| P1 存档+选关+UI | Claude Code | save.py 存档/解锁/最佳分、选关界面、继续游戏、霓虹配色 UI | 规则层 11 项 + 冒烟通过 | 待 ChatGPT 审查 |
| E6/E8/E9 | Claude Code | 摄像机缩放平移、自制音效 + audio.py、pathing.py + PyInstaller 配置 | 规则层 + 冒烟通过 | 待 ChatGPT 审查 |

---

## 3. 作业要求对照

按 job_description 逐项核对当前落地状态；与 §4 自测分工，本表只做「要求 → 落地」追溯。

### 3.1 基本要求（§三.1，11 条）

| # | 要求 | 落地（增量 / 代码） | 状态 |
|---|---|---|---|
| 1 | 可显示、可操作的图形界面 | I1 窗口 + 棋盘 | ✅ |
| 2 | 上下左右四向箭头 | I1 `draw_arrow` | ✅ |
| 3 | 鼠标点击选箭 | I1 命中检测 | ✅ |
| 4 | 判断前进方向是否有箭 | I2 `rules._ray` | ✅ |
| 5 | 无挡飞出并消失 | I2 `FlyOut` | ✅ |
| 6 | 有挡不消失 + 碰撞反馈 | I2 `Blocked` | ✅ |
| 7 | 失误次数 + 界面显示 | I2 顶栏余次 | ✅ |
| 8 | ≥3 可通关关卡 | I3 4 关（levels.json） | ✅ |
| 9 | 清空显示通关 + 进下一关 | I3 `ResultScene` pass | ✅ |
| 10 | 次数耗尽显示失败 | I3 `ResultScene` fail | ✅ |
| 11 | 重新开始恢复初始 | I3 重开按钮 `reset` | ✅ |

### 3.2 界面 / 过程要求（§三.2–6）

| 要求 | 状态 |
|---|---|
| 开始 / 游戏 / 结果三画面 | ✅ |
| 显示关名 / 棋盘 / 余箭 / 余次 / 重开 | ✅ |
| AIGC ≥3 次记录 | ✅（7 条，见 §2） |
| 测试 T01–T06 | ✅（自动化 + 冒烟，见 §4） |
| GitHub + README + 多次 commit | ⏳ 待 I4 |
| 博客 + PSP | ⏳ 待 I4（PSP 部分回填） |

### 3.3 评分对照（§四）

| 评分项 | 分值 | 现状 |
|---|---|---|
| 游戏界面与基本交互 | 10 | 已达成 |
| 箭头与路径判断 | 25 | 已达成 |
| 飞出、碰撞与失误机制 | 15 | 已达成 |
| 关卡与游戏流程 | 15 | 已达成 |
| AIGC 使用与分析 | 15 | 已达成 |
| 项目测试 | 5 | 已达成 |
| GitHub 与 README | 5 | 待 I4 |
| 博客与 PSP | 10 | 待 I4 |
| 附加分（扩展） | ≤10 | 可选 |

### 3.4 可优化点（不阻塞基础分）

| 点 | 现状 | 优化 | 状态 |
|---|---|---|---|
| 受阻动画 | 仅左右晃动 | 前冲 → 碰撞 → 回位 | ✅ 已升级（E3） |
| 撤销 | 只有重开 | 加结算历史栈 | ✅ 已加（E2） |
| 关卡选择 | 只能顺序推进 | 标题加关卡选择 | ✅ 已加选关界面 |
| 提示 UI | `can_fly` 已就绪未接界面 | 接提示按钮 | 待做 |
| 音效 | 无 | 飞出 / 受阻短音 | 待做 |
| 打包 | 源码运行 | `pyinstaller` 出 exe | 待做 |

---

## 4. 对局自测

| 编号 | 操作 | 预期 | 实际 | 是否通过 |
|---|---|---|---|---|
| T01 | 点前方无挡的箭 | 飞出并消失 | 规则层 + 冒烟通过 | 是 |
| T02 | 点前方有挡的箭 | 不消失，余次 −1 | 规则层 + 冒烟通过 | 是 |
| T03 | 点贴边且朝盘外的箭 | 消失，无越界异常 | 规则层 + 冒烟通过 | 是 |
| T04 | 清完本关 | 通关并进入下一关 | 冒烟通过（清空→pass→下一关） | 是（冒烟） |
| T05 | 把余次点到 0 | 失败，可重开 | 冒烟通过（耗尽→fail→再战） | 是（冒烟） |
| T06 | 对局中途重开 | 布局与余次回到开局 | 冒烟通过（重开→快照复位） | 是（冒烟） |
| T07 | 撤销成功飞出 | 箭头恢复 | 规则层 `test_undo_fly` 通过 | 是 |
| T08 | 撤销失误扣次 | 余次恢复 | 规则层 `test_undo_blocked` 通过 | 是 |
| T09 | 所有关卡可解 | solver 有完整解序 | 规则层 `test_solver_all_levels` 通过 | 是 |
| T10 | solution 与规则一致 | 参考解清空本关 | 规则层 `test_reference_solution` 通过 | 是 |
| T11 | 存档 round-trip | 读回一致 + 解锁 + 最佳分 | 规则层 `test_save_roundtrip` 通过 | 是 |

> T01–T11 已由 `test_rules.py` + `test_smoke.py` 自动化覆盖；真实窗口下的视觉效果待本人实测回填。

---

## 5. 开发日志

| 时间 | 阶段 | 内容 |
|---|---|---|
| 2026-09-22 17:07 | 策划收尾 → 编码起步 | 策划 / 接口 / 关卡文档定稿；确认 pygame 2.6.1 接口名无错误、仅 SysFont 有运行期坑；修正 L1/L3 参考序；提炼范本提示，准备 I1 增量编码 |
| 2026-09-22 18:29 | I1 交付 | 建立文件框架（状态机+规则/表现分离，关卡 JSON 外置）；实现 I1「看得见点得着」；`test_rules.py` 7 项 + `test_smoke.py` 冒烟全过 |
| 2026-09-22 19:08 | I2 交付 | 实现核心循环：`try_clear` 接入点选，飞出滑动淡出、受阻晃动变色，动画期锁输入；规则层单测 + 冒烟全过 |
| 2026-09-22 19:23 | I3 交付 | 实现关卡闭环：结果画面（通过/失败/全通）、清空进下一关、余次耗尽失败、重开按钮；补全注释；规则层单测 + 冒烟全过 |
| 2026-09-22 22:04 | P0 扩展（E1–E3） | E1 关卡扩至 8 关 + solver.py（依赖图+拓扑排序）；E2 撤销历史栈；E3 受阻前冲返回动画；规则层 9 项 + 冒烟全过 |
| 2026-09-22 22:16 | E4 评分计时 | score.py 评分/星级、PlayScene 计时、结果画面显示得分/星级/用时/失误；修复大盘（9×10）超出窗口，cell 自适应 |
| 2026-09-22 23:12 | P1 存档+选关+UI | save.py 存档/解锁/最佳分；选关界面（按解锁进度+星级）；继续游戏（续关）；霓虹配色 UI；规则层 11 项 + 冒烟全过 |
| 2026-09-22 23:24 | E6/E8/E9 | E6 摄像机缩放/平移/归中；E8 自制 6 音效 + audio.py；E9 pathing.py + requirements/spec/build.bat；规则层 + 冒烟全过 |

---

## 6. 策划阶段总结

**时间线**（会话可观测，供 PSP 回填）：

| 时间 | 阶段 | 产出 |
|---|---|---|
| 09-21 19:38 | 需求 | 作业说明书解读（job_description.md） |
| 09-22 16:25–16:42 | 策划 | planning / levels / pygame / task / process 五份文档 |
| 09-22 17:04–17:07 | 收尾 | 元认知补充、pygame 核验、参考序修正 |
| 09-22 18:00–18:29 | 架构 + 编码 | 文件框架 + I1 + 测试 |

**成果清单**：

| 文件 | 内容 | 状态 |
|---|---|---|
| task.md | 任务规格（含底层逻辑提炼） | 定稿 |
| planning.md | 玩法 / 规则 / 判定 / 界面 / 增量 / 架构 | 定稿 |
| levels.md | 教程 + 三正式关，难度曲线 | 定稿（参考序已核） |
| pygame.md | Pygame 2.6.1 接口分表 + 主循环 + 场景骨架 | 定稿 |
| levels.json | 关卡数据（数据 / 规则分离） | 就绪 |
| game/*.py + main.py | 状态机 + 规则 / 表现分离代码框架 | I1 通过 |

**关键决策**：

| 决策 | 依据 |
|---|---|
| 架构 = 状态机 + 规则 / 表现分离 | 解谜小游戏，不硬套 MVC（planning §6） |
| 数据 / 规则分离，关卡 JSON 外置 | 改关改表不改判定 |
| 过程 = 增量（可玩切片） | 每片结束玩家可多做一件完整的事 |
| 边界判定「先问在不在盘内，再读占用」 | 负索引绕回会判错边缘朝外箭 |

**独立复核发现并修正**：

| 问题 | 处理 |
|---|---|
| L1 / L3 参考序与盘面不符（`b` 先出、`p1` 先出均被挡） | 改 levels.md 参考序 |
| pygame 2.6.1 `SysFont` 扫描注册表抛 TypeError | 改 pygame.md 用 `Font(path)` 直取 msyh.ttc |

**I1 交付（看得见点得着）**：main.py 主循环 + 标题→对局切换；按 levels.json 画网格与四向箭、顶栏（关 / 余箭 / 余次）；点箭高亮、点空白无反馈。规则层单测 7 项 + 冒烟全过。

**下一增量**：I2 核心循环——把 `try_clear` 结果接到点选上，飞出滑动、受阻晃动，余次显示跟手。

---

## 7. 代码框架

随增量同步更新：新增 / 改动模块时，同步此图与依赖表。

```text
AIGCGame/
├── main.py                # 入口：App 容器 + 「取输入→推进→画一帧」主循环
├── levels.json            # 关卡数据（数据 / 规则分离，改关改表不改判定）
├── game/                  # 游戏代码包
│   ├── __init__.py
│   ├── rules.py           # 规则层：盘面 / 射线 / 余次 / 快照 / 撤销历史（不 import pygame）
│   ├── solver.py          # 规则层：依赖图 + 拓扑排序求解 / 关卡分析（不 import pygame）
│   ├── score.py           # 规则层：评分 / 星级（不 import pygame）
│   ├── save.py            # 数据层：本地进度存档（仅标准库）
│   ├── audio.py           # 表现层：音效统一入口（加载失败不阻塞）
│   ├── pathing.py         # 工具：资源路径（兼容打包）
│   ├── levels.py          # 数据层：读 levels.json
│   ├── view.py            # 表现层：摄像机缩放/平移、棋盘 / 箭头 / HUD / 按钮绘制
│   ├── anim.py            # 表现层：飞出 / 受阻前冲返回动画
│   └── scenes.py          # 表现层：场景状态机（标题 / 选关 / 对局 / 结果）
├── test_rules.py          # 规则层单元测试（不依赖 pygame）
├── test_smoke.py          # 无窗口冒烟测试（dummy 驱动）
└── notes/                 # 策划 / 过程文档
    ├── task.md            # 任务规格（含底层逻辑提炼）
    ├── planning.md        # 策划案
    ├── levels.md          # 关卡设计源
    ├── pygame.md          # Pygame 2.6.1 接口分表
    ├── process.md         # 本文件：过程记录
    └── job_description.md # 作业说明书
```

**依赖方向（单向向下，规则层不回头依赖表现层）：**

| 层 | 模块 | 依赖 |
|---|---|---|
| 入口 | main.py | pygame、scenes、levels |
| 表现层 | scenes.py | pygame、view、rules |
| 表现层 | view.py | pygame、rules |
| 表现层 | anim.py | pygame |
| 规则层 | rules.py | 无（纯逻辑） |
| 规则层 | solver.py | rules（DIRS） |
| 规则层 | score.py | 无（纯逻辑） |
| 数据 | save.py | 标准库 json / os |
| 数据 | levels.py | 标准库 json / os |

**约束**：表现层只读规则层结果并画出来；规则层绝不 import pygame；关卡坐标只进 levels.json，不写死在规则里。

**落地进度**：I1–I3 基础功能完整，P0（E1 关卡+solver、E2 撤销、E3 受阻动画）、E4 评分、E5 存档、选关界面、E6 摄像机、E7 霓虹 UI、E8 音效、E9 打包配置均已通过。剩余：E9 实际构建验证、I4（回归 + README + 截图 + 博客）。